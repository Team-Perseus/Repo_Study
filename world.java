import java.util.PriorityQueue;

public class world {
    static int shake(int[] scovile, int k){
    int cnt=0; 
    int a=0;b=0;
    PriorityQueue<Integer> queue = new PriorityQueue<Integer>()
    for(int i = 0; i<scovile.length; 1++) queue.offer(i);
    while(queue.size()>1){
        
    }
    if(queue.peek()<k) return -1;
    return cnt;
}
public static void main(String[] args) {
   
}
